{
  "name": "simple-game-engine",
  "version": "1.0.0",
  "description": "Just a simple game engine",
  "scripts": {
    "watcher": "nodemon --ignore node_modules --watch lib --watch example --watch bin",
    "examples": "PORT=3000 npm run watcher -- --exec \"ts-node bin/server.ts\" bin/server.ts"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "express": "^4.18.2",
    "nodemon": "^3.0.2",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.3.3"
  }
}
